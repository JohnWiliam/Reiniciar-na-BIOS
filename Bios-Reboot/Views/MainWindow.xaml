<ui:FluentWindow x:Class="BiosReboot.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:BiosReboot.ViewModels"
        mc:Ignorable="d"
        Title="Confirmar Reinicialização"
        Height="250" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        Icon="/Assets/icon.ico"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=False}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Reiniciar na BIOS" />

        <StackPanel 
            Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
            <ui:SymbolIcon Symbol="Power24" FontSize="48" Margin="0,0,0,15" Foreground="{DynamicResource SystemAccentColorBrush}" />
            <TextBlock Text="Deseja reiniciar agora e entrar na BIOS/UEFI?"
                       FontSize="16" 
                       FontWeight="SemiBold"
                       TextAlignment="Center" 
                       TextWrapping="Wrap"/>
            <TextBlock Text="(Firmware Setup)" 
                       FontSize="14" 
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       TextAlignment="Center" 
                       Margin="0,5,0,0"/>
        </StackPanel>

        <Grid Grid.Row="2" Margin="20" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <ui:Button Grid.Column="0" 
                       Content="Cancelar" 
                       Icon="Dismiss24"
                       Command="{Binding CancelCommand}"
                       HorizontalAlignment="Stretch" 
                       Appearance="Secondary"/>
            
            <ui:Button Grid.Column="2" 
                       Content="Reiniciar" 
                       Icon="Checkmark24"
                       Command="{Binding ConfirmRestartCommand}"
                       HorizontalAlignment="Stretch" 
                       Appearance="Primary"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
